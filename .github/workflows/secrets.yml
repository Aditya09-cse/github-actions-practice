name: Secrets Message

on:
  workflow_dispatch:
    
jobs:
  secrets:
    runs-on: ubuntu-latest
  
  steps:
    - name: print secret
      run:  echo "the secret is:" ${{ secrets.MY_SECRET_MESSAGE }}